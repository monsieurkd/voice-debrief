# Voyo — Progress Update · 2026-09-01 (Tue)

> **Loom:** no link yet — the UI makeover is committed and worth a quick
> recording, but I haven't recaptured a screen capture.

- **progress** (from the last update):
  - Committed the full Voyo glass makeover (`444b42e`): chat surface rebuilt on
    the deep-blue glass canvas — glassed assistant bubbles, deep-blue gradient
    user bubbles, floating composer dock, animated orb "listener" avatar,
    symmetric mic + send, auto-speak TTS.
  - Wired the persona/voice picker (3 voices, `src/lib/personas.ts`) into the
    sidebar and added a per-conversation JSON export entry point
    (`/conversations/:id/export`). Fixed the New-chat button (monotonic session
    key forces a real remount).
  - Fixed the CSS build error: Tailwind v4 was auto-scanning `.md` docs whose
    prose code samples (`text-[var(...)]`) compiled into broken `var(...)`
    utilities. Scoped the `@tailwindcss/postcss` plugin to `./src`. Local dev +
    production build are now clean.
  - Verification: 55/55 tests green; `typecheck` and `lint` clean; prod build
    green. Working tree clean after commit.
- **todo** (upcoming plans):
  - Mobile/navigation pass — the sidebar is hidden on phones (`hidden sm:flex`),
    so history, voices, and export are unreachable on small screens. Add a
    drawer/menu for mobile.
  - Auto-speak control — add a stop/mute affordance for the automatic TTS
    replies.
- **notes** (from the meeting):
  - Chosen directions: animated orb = assistant avatar; subtle sidebar voice
    picker; export button on each chat row.
  - Watch-outs to address later:
    - Message bubbles keyed by array index (`key={i}`) — fragile for reconcile/
      reorder.
    - A new conversation isn't highlighted in the sidebar until a page reload.
    - The primary-blue gradient (`#2757c9 → #3670f0`) is duplicated across a few
      files; consolidate into one theme token.
